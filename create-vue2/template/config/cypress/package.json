{
  "scripts": {
    "preview": "vue-cli-service serve --port 10086",
    "test:e2e": "start-server-and-test preview http://localhost:10086/ 'cypress open --e2e'",
    "test:e2e:ci": "start-server-and-test preview http://localhost:10086/ 'cypress run --e2e'"
  },
  "devDependencies": {
    "cypress": "^10.3.0",
    "start-server-and-test": "^1.14.0"
  }
}
