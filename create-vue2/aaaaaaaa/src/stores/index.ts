import Vue from 'vue'
import Vuex from 'vuex'

const state = {}
const getters = {}
const mutations = {}
const actions = {}

Vue.use(Vuex)

export default new Vuex.Store({
  state,
  getters,
  actions,
  mutations
})
